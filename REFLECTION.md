# 项目反思

## 方法论真正发挥作用的部分

这次项目里作用最大的 Superpowers 技能不是某个“生成代码”的技巧，而是把判断节点强制显式化的几项流程。brainstorming 迫使项目先回答 harness 的主角究竟是什么。最初很容易把注意力放在接一个真实模型、做一个能聊天的页面上，但讨论后把主要贡献收束到治理：结构化 action、workspace 边界、确定性 guardrail、HITL 状态和审计。writing-plans 的价值则是把这些抽象名词拆成可以独立验收的接口和测试，使 mock LLM 被移除后，机制仍然存在并可验证。

这个转向也改变了我对 coding agent 的理解。一个 agent 看起来是否聪明，并不等于它是否值得信任。
真正能交付给用户的部分，是当模型犯错、输出含糊、尝试越界或要求危险操作时，系统是否还有
稳定的边界。Guarded Harness 的设计把“智能”放在 provider 层，把“责任”放在 harness 层：
provider 可以替换，治理路径不能绕过。

test-driven-development 对 AI 协作是放大器，不是单纯的限制。最终复审暴露的 shell 绕过很说明问题：原实现已有不少 guardrail 测试，看起来覆盖了危险命令，但没有执行级测试证明 `subprocess` 收到的究竟是字符串还是 argv。先写 `echo $(rm file)`、换行、反引号、重定向、管道和 symlink 的失败测试后，问题从“再补几个正则”变成了清晰的架构事实：策略层与执行层必须共享解析结果，executor 也必须独立 fail-closed。红灯在这里不是仪式，而是防止智能体用一个表面补丁掩盖根因的证据。

verification-before-completion 同样重要。项目最后用 Python 3.11 虚拟环境运行完整
`pytest -q`、`compileall src tests` 和 diff 检查，避免只凭局部测试判断完成。相对而言，
部分流程记录会显得形式大于实质，例如每个很小的文档改动都重复同样的技能声明，信息增量有限。更好的做法是保留关键决策、失败证据和人工取舍，而不是堆叠过程口号。

在课程项目里，这种证据意识尤其重要。AI 可以很快生成一套看似完整的结构，但“看似完整”
和“被验证过”之间差了一整个工程过程。最终我更看重三类证据：失败测试是否真的失败过、
修复是否覆盖了根因、文档是否没有超过代码真实能力。

## subagent、任务颗粒度与规约质量

subagent-driven 工作流在职责边界清楚时可以持续推进较久，例如 store、guardrail、dispatcher 各自拥有明确输入输出，子任务容易在一次会话内实现、测试和评审。最合适的颗粒度不是“改一个文件”，而是“交付一个可观察行为及其回归测试”。颗粒度太大时，安全、状态和 UI 会被一次性混合，评审难以定位；太小时，agent 只机械搬运代码，丢失端到端语义。

SPEC/PLAN 的清晰度直接决定实现质量。一个具体偏差是：规格写了 shell action 受 workspace 约束，却没有明确禁止任意字符串 `shell=True`，也没有规定 shell 元字符的语义。实现者于是用正则和 shlex 做分类，最终仍把原字符串交回 shell，形成 policy 与 executor 之间的绕过。另一个偏差是“approve 后恢复执行”没有定义跨进程 live provider 的上下文如何保存，CLI/Web 便用 MockLLM finish 填补空白，用户可见语义与真实能力不一致。复审后的修正不是假装已经有 provider 持久化，而是明确：跨进程命令只处理审批动作并结束恢复轮次。

最有效的 prompt/context 策略是同时给出威胁样例、必须保持的正常场景和验证命令。只说“加固 shell”容易导致过度封禁；同时列出 `git status --bad-option` 必须执行并返回 command_error、pytest 与只读 git 子命令仍可用，就把安全目标与兼容边界都固定下来。要求“不回滚已有改动”和指定 worktree 也很关键，它让 agent 在共享代码库中先读状态再编辑，而不是把整洁误认为正确。

这也说明，SPEC 不是写给评审看的装饰，而是写给未来实现者和未来自己的约束。越晚补清楚
术语，越容易在实现里留下语义债。例如 approval 的“deny”一开始既可以表示“拒绝这个动作，
让模型换路线”，也可以表示“停止整个任务”。等到 WebUI 真正可用时，这种含糊就会变成用户
体验问题。最后拆成三个按钮后，系统行为和用户意图才重新对齐。

## 凭据、分发与可靠性取舍

凭据要求迫使项目区分“执行需要的值”和“允许持久化的表示”。审批动作可能包含 `.env` 内容，若模板直接展示 `action_json`，keyring 做得再好也会在 WebUI 和 SQLite 链路泄露。当前方案在审批 action 入库前脱敏，Web、CLI 和审计也复用同一套 redaction；代价是含 secret 的审批动作批准后只会执行脱敏值，真正的 secret 必须来自 keyring 或本地环境。这仍不是理想终点；更强的方案应使用系统保护的加密密钥加密 action，或把 secret 引用化而不持久化值。

分发要求则揭示“本地能跑”和“别人能可靠获得”不是同一件事。Dockerfile 解决运行环境复现，
GitHub Actions 解决每次提交后的自动测试。若重做项目，我会更早建立 Python 3.11 的完整依赖环境和最小 CI，在功能增长前就跑 Web/CLI 与容器冒烟测试；同时会先设计审批 outbox 和 provider checkpoint，而不是在最后补跨进程恢复语义。

人工评审在这个过程中不是给智能体产物做表面确认，而是寻找“测试与承诺之间的空白”。例如原有测试证明策略函数会拒绝若干路径，却没有证明执行器不再解释 shell 语法；审批测试证明 action 能恢复，却没有检查展示层和磁盘层保存了什么。真正有效的评审问题应是：还有哪一层使用了未经验证的表示，崩溃发生在两次写入之间会留下什么状态，用户看到的文案是否超过系统真实能力。这样的追问比单纯增加覆盖率数字更能提高可靠性。

如果重做一次，我会把三个设计提前：第一，shell command 从第一版就建模为 argv，而不是先接受
字符串再逐步收紧；第二，approval outbox 和 execution status 从第一版就按事务边界设计，
避免后面再补 `executing/executed/failed`；第三，live provider 对话 checkpoint 要么明确
不做，要么从一开始设计持久化格式，不让 CLI 和 WebUI 在恢复语义上出现灰区。

我也会更早准备提交材料。README、SPEC、PLAN、PROCESS、LOG、REFLECTION 各自有不同读者：
README 面向运行者，SPEC 面向评审者，PLAN 面向实现者，PROCESS/LOG 面向过程证据，
REFLECTION 面向学习总结。如果这些文档最后才补，很容易互相重复或口径不一致；这次整理后，
它们的分工才比较清楚。

## 对 Superpowers 的批判

Superpowers 假设需求可以被逐步澄清、测试能表达主要风险、任务边界足以隔离并行工作，而且开发者有能力判断何时流程产物只是形式。这些假设在核心机制上大体成立，但在跨进程副作用和真实 provider 状态上只部分成立。TDD 能证明状态转换和脱敏展示，却不能提供 shell 副作用的 exactly-once 保证；CI 能重复运行测试，但不能代替对风险模型的人工判断。方法论最有价值的地方，是持续要求证据并暴露这些边界。它最危险的地方，是丰富的 checklist 可能制造“流程已走完，所以系统可靠”的错觉。最终责任仍在工程师：识别模型和流程没有覆盖的威胁，拒绝虚假的完成叙事，并把剩余风险写得足够具体。

所以我对 Superpowers 的最终评价是：它适合用来防止 AI 开发失控，但不能替代工程判断。
它能提醒我先澄清需求、先写测试、先验证再宣布完成；但哪些风险值得测试、哪些功能应该砍掉、
哪些文档表述会误导评审，仍然要由人判断。这个项目最有价值的收获，就是把 AI 当成高效协作者，
而不是把责任外包给 AI。
