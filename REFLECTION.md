# 项目反思

这次做 Guarded Harness 之前，我对 coding agent 的理解其实比较直接：接上一个模型，让它能读写文件、运行命令，再做一个 Web 页面，就差不多是一个能工作的 agent。真正做完以后，我发现核心并不在“模型能不能做事”，而在“模型出错时系统能不能拦住”。所以这个项目最后最重要的部分，不是聊天界面，也不是接入真实 provider，而是我自己写的 harness 层：解析 action、检查风险、执行工具、记录审计、等待人工审批，再把失败反馈回到下一轮。

Superpowers 对我最大的帮助，是让我一开始没有直接冲去写代码。brainstorming 阶段虽然有点慢，但它逼我先想清楚项目的主角到底是什么。最初我容易把重点放在模型能力和页面体验上，后来才把方向收束到治理机制：LLM 只能提出结构化 action，真正执行前必须经过代码判断。这个边界一旦定下来，后面的 SPEC、PLAN 和测试都清楚了很多。

TDD 这次也不是单纯为了满足流程要求。印象最深的是 shell 执行问题。早期我已经写了 guardrail 测试，比如拒绝 `rm -rf /`、让 `git push` 进入审批，看起来好像安全了。但复查时发现，如果执行层仍然把字符串交给 shell 解释，就可能绕过前面的判断。后来我补了管道、重定向、命令替换、换行和 symlink 等用例，才把实现改成 argv 解析和 `shell=False`。这让我意识到，测试真正有价值的地方，是能暴露“看起来安全”和“实际安全”之间的差距。

subagent-driven 的体验有好有坏。像 store、guardrail、dispatcher 这种边界清楚的模块，agent 能比较稳定地推进，因为每个任务都有明确输入输出，也有测试能验证。但任务太大时，比如 WebUI、live provider、凭据处理混在一起，agent 就容易把很多语义搅在一起。最后我觉得比较合适的任务颗粒度，是“交付一个可以被测试或演示证明的行为”，而不是简单按文件数量拆分。

SPEC 和 PLAN 的质量对实现影响很直接。写清楚的地方，后面实现就顺；没写清楚的地方，最后一定会回来补债。比如一开始我只写 shell action 要受 workspace 限制，却没有说清楚 shell 字符串怎么执行，结果留下了绕过空间。审批也是一样，最初的 deny 含义不明确，到底是拒绝当前动作，还是停止整个任务？后来 WebUI 拆成 `Approve once`、`Deny action`、`Stop task` 三个按钮，行为才和用户意图对齐。

凭据和分发要求也让我补上了以前容易忽略的工程细节。API key 不能只是不写进源码，还要考虑它会不会进入日志、SQLite、审计事件或页面展示。后来我把 provider 配置和 key 分开：非敏感配置写入 `.guarded-harness/provider.json`，key 通过 OS keyring、环境变量或当前 Web 进程临时保存。Docker、wheel、GitHub Actions 和线上 WebUI 也让我意识到，“我本机能跑”和“别人能稳定跑起来”不是一回事，分发本身就是工程的一部分。

如果重做一次，我会更早做三件事：第一，从第一版就把 shell command 建模成 argv，而不是先接受字符串再补限制；第二，更早设计 approval 的状态机和事务边界；第三，提前明确 live provider 对话是否需要持久化，不让 CLI 和 WebUI 的恢复语义看起来超过真实能力。

总的来说，这次项目让我对 AI4SE 的理解更实际了。AI 可以很快帮我写代码、补测试、整理文档，但它不能替我判断系统是否真的安全、是否满足要求、是否把风险说清楚。Guarded Harness 给我的最大收获是：当 AI 参与越来越多时，工程师反而更需要负责边界、证据和取舍。
